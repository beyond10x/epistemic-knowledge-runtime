# P1-08 — Kernel-validated seeding

Skill: aep-drive:wave 0.9.3. The operator's approved completion plan and
"Implement the plan" authorize source commits, integration and publication. This is a
single implementation unit, story:kernel-validated-seed, followed by adversarial review.
The kernel writer, persisted-contract versioning and explain remain subsequent units.

## Selection and preflight

Base main: 250aeca, the merged and checked transaction membrane repair. Main is clean.
The complete selection output is [p1-08-selection.json](p1-08-selection.json). It identifies
the seed unit as the next dependency-ready wave; its recorded collisions prevent concurrent
implementation of the persisted-contract and writer units. Inferred scope additions are new
kernel seed code and fixtures; the preparation and evidence-scope reports cite existing paths.

The primary and continuing coordinator checkout are the only existing trees. The previous
unit's source tree and dedicated compiler output have been retired. The coordinator build
directory remains in active use. Disk observations are appended by the opening command;
new compilation pauses below the approved 10 GB floor. The measured previous unit cleanup
removed 1.9 GiB of disposable output, as reported by cargo clean. Machine Cargo configuration
already enables sccache. No new model budget was supplied; no cost estimate is claimed.

| owner | managed tree / branch | build / scratch | lease |
|---|---|---|---|
| coordinator | `<worktrees>/ekr-completion-20260922`; `codex/ekr-completion-20260922` | `<cache>/b10x-target/ekr-completion-20260922`; `<cache>/ekr-completion-20260922` | codex-ekr-completion-20260922 |
| seed | `<worktrees>/ekr-p1-08-seed`; `codex/ekr-p1-08-seed` | `<cache>/b10x-target/ekr-p1-08-seed`; `<cache>/ekr-completion-20260922/seed` | worker acquires and reports its own |

The host does not expose plugin subagent_type dispatch. The worker and reviewer explicitly
load the installed aep-drive:implementor and aep-drive:adversary charters. The worker receives
[p1-08-seed-brief.md](p1-08-seed-brief.md); the coordinator owns ESS and planning writes.

## Coordinator checks before dispatch

The Seed invalid-seed outcome is declared before the unit tree is forked, so both trees share
the same contract. Its behavioral claim is explicitly unexecuted until the new cases run.
No newly added source-reading guard may use a compile-time manifest path. Existing guard
debt is already recorded and is not evidence for this wave's seed semantics.

The archived dangling-reference, undeclared-type and caller-supplied Accepted witnesses
become kernel refusal regressions through both providers. Their old APIs may disappear;
the behavioral claims survive. The store case asserting successful dangling conversion
stages the defect and is replaced. Provider-only tests retain explicit substitute-authority
coverage and cannot establish kernel admission. Valid filing, float, transient and self-parent
assertions survive at the kernel boundary. Empty bootstrap is an explicit valid control;
ordinary empty transactions remain refused. See the brief for all remaining acceptance cases.

The opening receives format, ESS, planning and privacy checks before dispatch. The unit
receives package tests and clippy, then independent adversarial cases. Integration receives
every gate step with a separately captured status. No unavailable agent counters are invented.

## Opening observations

Free filesystem space at opening (GiB-rounded):

31G

Coordinator compiler output: 3.4G

## Implementation handback and review

The implementor returned a stable uncommitted unit and released its own lease.
[Implementation report](../reviews/p1-08-seed-implementation.md) and
[exact old-to-new test map](../reviews/p1-08-seed-case-migration.md) retain its evidence.
The report quotes the touched-package lane rising from 203 to 217 executed cases;
five red witnesses preceded the fixes. Ownership checks, format, package clippy and
package documentation passed. These are unit results, not the integration gate.

Independent seed review is active in that same unit tree, with its own lease and
scratch `<cache>/ekr-completion-20260922/seed/adversary`. The reviewer receives the
[seed adversary brief](p1-08-seed-adversary-brief.md). Source changes are held until
the reviewer returns. No unavailable harness cost counters are claimed.

## Review correction

[Independent review](../reviews/p1-08-seed-adversary.md) returned a reproducible
blocker: misfiled ontology property IDs survive real kernel seed admission and
restart. Its suite reports 220 passed and one failed. The coordinator verified
the actual caller and the values-only shared loader before routing correction.
`review-result:p1-08-seed-adversary` preserves the finding with historical origin
undecided; unchanged loader source alone does not execute the old seed path.

The reviewer released its lease. Correction is active in the seed unit, adding
the shared ontology schema/error check and exact loader regressions. Scope now
includes `crates/ekr-ontology/src/schema.rs` and `tests/ontology_load.rs` there.
The independent kernel case retains actual malformed seed submission against
valid compatibility configuration and checks that refusal writes nothing.
Independent correction verification and full integration remain pending.

## Closure evidence

Source commit: 0fb7951c6d5d5803493d33d80a34c53c7070fe4d.
[Correction report](../reviews/p1-08-seed-correction.md) and
[independent correction verification](../reviews/p1-08-seed-correction-review.md)
close the ontology property-filing blocker. Original historical origin remains
undecided. Owners: one implementor finding corrected; no new correction-review
findings, as reported by the independent review.

The coordinator mutation bypassed only the property-key equality check. The
unchanged independent kernel case failed (exit 101), naming malformed node and
edge properties admitted through both File and SQLite. The original source was
restored before the integration gate. No test constructor panic or skip was
accepted as evidence.

Integrated gate: **403 passed, 0 failed, 0 ignored**. All steps exited zero:
fmt-check 0
clippy 0
test 0
doc-check 0
spec-check 0
plan-check 0

Small raw logs remain at <cache>/ekr-completion-20260922/seed/integration-*.log
and coordinator-mutation.log; the durable reports retain the outcome and case.
Source-reading guards introduced by this unit use runtime paths; the existing
compile-time path debt remains scheduled. Kernel ESS Seed comments name retained
ontology without claiming the future populated ontology root. Post-seed writer,
format versioning, provider blob placement and preserving migration remain open.
Required remote checks and publication are the next integration step.
