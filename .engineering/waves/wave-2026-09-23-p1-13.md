# Wave p1-13 — explain and the `ekr` binary

Skill: aep-drive:wave 0.9.3. Coordinator: Claude session `claude-579ad7f9-p1-13`.
Status: **approved and running.** Approval: the operator approved the five-wave plan on
2026-09-23 (`<home>/.claude/plans/golden-jingling-kite.md`, wave 1). Base: `main` @ `e41a99b`.

## Units

| unit | story | branch | tree id | build dir | scratch | stage |
|---|---|---|---|---|---|---|
| p1-13-explain | `story:seed-and-explain` | `wave/p1-13-explain` | `p1-13-explain` | `<cache>/b10x-target/ekr-p1-13-explain` → `/dev/shm/ekr-p1-13/p1-13-explain` | `<cache>/ekr-p1-13/p1-13-explain` | dispatched |
| p1-13-cli | `story:ekr-cli` + `task:the-proposer-field-is-unauthenticated` + CLI half of `task:conformance-command-input-contract` | `wave/p1-13-cli` | `p1-13-cli` | `<cache>/b10x-target/ekr-p1-13-cli` → `/dev/shm/ekr-p1-13/p1-13-cli` | `<cache>/ekr-p1-13/p1-13-cli` | dispatched |
| integration | — | `wave/wave-p1-13` | `wave-p1-13` | `<cache>/b10x-target/ekr-wave-p1-13` → `/dev/shm/ekr-p1-13/wave-p1-13` | `<cache>/ekr-p1-13` | open |

Both serve `vision:o2`. Implementors `aep-drive:implementor`, adversaries `aep-drive:adversary`
(two passes). Briefs: `<cache>/ekr-p1-13/brief-{common,p1-13-explain,p1-13-cli}.md`.

**Dependency inside the wave.** The CLI's `snapshot` and `explain` verbs consume the kernel
carriers the explain unit builds. The CLI unit builds its four write verbs and the § 65 example
first; after the explain unit merges, the coordinator fast-forwards the CLI tree and the CLI unit
finishes the two read verbs in a follow-up round.

**Selection.** `aep plan artifact waves` puts the three draft stories in three waves; the only
collision between `seed-and-explain` and `ekr-cli` is `systems/ekr/domains/kernel.yaml`, which is
coordinator-owned and not edited by either unit. `ess-conformance-kernel` is wave p1-14.

**Split task.** The ESS half of `task:conformance-command-input-contract` (a retained Transactions
view, real document-path inputs in `kernel.yaml`) moves to wave p1-14 with the conformance target.

## Pre-flight, 2026-09-23

| check | value | source |
|---|---|---|
| primary | `main` @ `e41a99b`, clean, equal to `origin/main` | `git status -sb` |
| worktrees before | primary only | `git worktree list` |
| `/` free | 92G | `df -h /` |
| `/dev/shm` free | 32G | `df -h /dev/shm` |

## Commits the approval authorises

One commit per unit round, the bot-authored merge of each unit into `wave/wave-p1-13`, this page's
commits, the closing store commit, the bot push, the pull request and its merge into `main`. No tag.

## Verb output, verbatim

`aep plan artifact waves --kind story --status draft --format json`, reduced by `jq`:

```json
{
  "waves": [
    {
      "wave": 1,
      "ids": [
        "story:seed-and-explain"
      ]
    },
    {
      "wave": 2,
      "ids": [
        "story:ekr-cli"
      ]
    },
    {
      "wave": 3,
      "ids": [
        "story:ess-conformance-kernel"
      ]
    }
  ],
  "collisions": [
    "story:ekr-cli|story:ess-conformance-kernel|Cargo.lock|cited",
    "story:ekr-cli|story:ess-conformance-kernel|crates/ekr/Cargo.toml|cited",
    "story:ekr-cli|story:ess-conformance-kernel|systems/ekr/domains/kernel.yaml|cited",
    "story:ekr-cli|story:seed-and-explain|systems/ekr/domains/kernel.yaml|cited",
    "story:ess-conformance-kernel|story:seed-and-explain|systems/ekr/domains/kernel.yaml|cited"
  ],
  "unassessed": []
}
```
