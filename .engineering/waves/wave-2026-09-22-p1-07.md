# P1-07 — Repair the transaction membrane

Skill: aep-drive:wave 0.9.3. Interactive run; the operator's approved completion plan and
"Implement the plan" authorize this wave, its source commits, integration and publication.
This is not the 0.1.0 release.

## Selection

One implementation unit: `story:p1-transaction-membrane-repair`, serving O2.
The coordinator handles evidence preservation, planning reconciliation and correctness CI.
Downstream seed and writer work wait for the membrane checkpoint.

The pre-activation `aep plan artifact waves --kind story --status draft --format json`
returned these artifact groups, with `collisions: []`, `unassessed: []` and `cycles: []`:

1. `story:commit-and-revision-lineage`, `story:p1-transaction-membrane-repair`
2. `story:seed-and-explain`
3. `story:ekr-cli`
4. `story:ess-conformance-kernel`

File disjointness does not establish semantic readiness: the writer remains blocked and is
not selected. The scoper found the membrane surface directly in the validation modules.
Only the possible new candidate-index helper is inferred.

## Preflight and recovery

Base: `209dd5e`. The primary has only the operator-supplied untracked handoff; integration
preserves a copy and does not alter the primary file. No previous wave worktree is active.
Use one implementation worker and an adversary afterward, with a dedicated target directory.
The previous independent review measured one fresh build at approximately 2.8 GiB; the
operator's disk floor is 10 GB. Check free disk before builds and on worker handoff.
No new model token budget was supplied; no token estimate is claimed.

The available collaboration tool has no plugin subagent_type selector. Workers explicitly load
the installed `aep-drive:story-scoper`, `aep-drive:implementor` and `aep-drive:adversary`
charters instead; they are not represented as native plugin-agent dispatches.

| owner | tree / branch | build / scratch | stage |
|---|---|---|---|
| coordinator | `<worktrees>/ekr-completion-20260922`; `codex/ekr-completion-20260922` | `<cache>/b10x-target/ekr-completion-20260922`; `<cache>/ekr-completion-20260922` | draft PR 7 at `0a9f31c` |
| membrane | `<worktrees>/ekr-p1-07-membrane`; `codex/ekr-p1-07-membrane` | `<cache>/b10x-target/ekr-p1-07-membrane`; `<cache>/ekr-completion-20260922/membrane` | implementation and package verification |

## Observed interruptions and related integration

Unrelated workspace builds reduced free disk below the approved floor. The coordinator's
first full gate was terminated with exit 143; it is not green evidence. Its exact reproducible
build directory was reclaimed after all processes in its process group exited. The gate log
remains at `<cache>/ekr-completion-20260922/baseline-with-ci-gate.log`. The worker paused new
compiler invocations below the floor and resumed after space recovered.

The private policy enrollment was integrated separately through its PR 1, merged at
`8520a433221ee34e1a7284789f58fd242229e97b`. The original policy checkout's unrelated changes
and backup files remain untouched. The dedicated policy worktree was finished and removed
through reviewed exact-id GC.

The two apparent stale projection tasks retain real implementation gaps. Their corrected
comments do not prove complete projections, so they remain open with the residual recorded.
The original second report has seven numbered findings containing multiple reproduced defects;
its preserved text, rather than the handoff's different count, is the finding inventory.

## Acceptance classification

The seven transaction witnesses become named refusal tests. The valid transaction and
unconstrained lifecycle controls survive. Fresh schema definition and MergeEntity positives
become explicit unsupported-operation cases, following the approved P1 phase boundary;
their malformed-operation and identity cases remain.

The retained seed and hash/projection cases belong to later units and are archived without
pretending this wave executes them. No new source guard may use a compile-time checkout path.

## Gate and review

Package tests, clippy and format on the implementation tree; adversarial cases there after
implementation; full `task check` on the integrated tree. Record command exit statuses, executed
counts and one disposition for each finding. Record the independent membrane review before
starting the seed wave. The coordinator's opening changes receive format, ESS, plan and privacy
checks before dispatch.

## Independent checkpoint correction

The unit adversary returned no findings; its report is recorded as
review-result:p1-07-membrane-adversary. The integrated gate then caught the private candidate
helper's broad method visibility; the coordinator restricted it to pub(super) without changing
assertions. Every gate step subsequently exited zero. The retained gate logs are
<cache>/ekr-completion-20260922/membrane-*.log, with per-step statuses in membrane-gate-status.txt.

The separate independent checkpoint found a pre-existing unsupported ontology effect that serde
silently discards. It is recorded in review-result:p1-07-independent-checkpoint and routed to
story:refuse-discarded-ontology-semantics before seed admission starts. The same worker tree is
reused sequentially for this bounded decoder repair, under p1-07-decoder-brief.md; there are no
concurrent edits to it. Its prior source commit is 6328031 and the integration merge is fb68c28.

A coordinator mutation removed candidate edge deletion in the unit tree. The named deleted-edge
assertion regression failed with exit 101; the source was restored by an exit trap and verified
clean. This measures that the regression detects the repaired post-state semantics.
