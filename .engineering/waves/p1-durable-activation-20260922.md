# Durable activation source unit

Skill: aep-drive:wave 0.9.3, continuing the operator-approved completion plan.
N = 1 source owner for story:version-persisted-contracts and
story:commit-and-revision-lineage, both serving vision:o2. The coordinated
ownership exception and format/seed then application milestones are already
recorded in p1-durable-activation-brief.md and both story bodies.

The exact draft-story selection is retained verbatim in
p1-durable-activation-selection.json, including waves, collisions and unassessed
entries. The active format story joins its coupled writer in one unit. CLI,
Explain and conformance await this unit's real durable handlers. Recorded child
defects remain open implementation obligations within the unit; no decision or
acceptance is silently cleared by dispatch.

Managed id: ekr-durable-activation-20260922.
Branch: codex/ekr-durable-activation-20260922.
Tree: <worktrees>/ekr-durable-activation-20260922.
Target: <cache>/b10x-target/ekr-durable-activation-20260922.
Scratch: <cache>/ekr-completion-20260922/durable-activation.
Coordinator lease: codex-ekr-durable-activation-coordinator.
Worker lease: codex-ekr-durable-activation-implementor.

The isolated target is a task-owned symlink to disposable temporary-memory
storage. Its actual path is recorded privately in build-memory-root.txt. Evidence
and source remain on persistent storage. Use two compiler jobs, no incremental
output, no dev/test debug info, direct rustc and the assigned private TMPDIR.
Maintain the disk floor and inspect temporary-storage headroom before compilation.
Never share the coordinator target or clean another run's output.

The generic existing worker reads the aep-drive:implementor charter from the
installed plugin. This harness has no subagent_type dispatch field; that is the
explicit dispatch deviation. No second implementor owns overlapping source.

The exact released-compiler-qualified ESS patch is applied to coordinator and
unit together before implementation. The coordinator owns those shared files,
AEP, Cargo pins and publication. The worker owns only the source/test scope in
the two stories and the implementation brief. No vendor or frozen parser-profile
changes are authorized. Current operation-shape integration remains required.

Checks 1–4: authoritative decisions name future executable witnesses in the
corrected acceptance table; that table maps surviving and replaced old cases.
Frozen original vectors/encoders and independent parser regressions survive.
Obsolete current projection claims move only with stronger real-owner checks.
Shared specifications are compared in both trees. The existing runtime-source-
location rule applies throughout this unit; no compile-time source locator is
admitted. No source success is asserted by the opening record.

The commit authorization is the approved unit source commit, source merges,
closing store records and integration into the base after required checks;
release/cutover remain governed by the approved overall completion contract.
Stage: shared declarations activated for source implementation. The exact patch
was applied to coordinator and unit together and all touched files compared.
Released ESS validation and compilation pass; this is specification evidence,
not a passing product gate. Production code now has to implement that contract.

Measured preflight: disk bytes available 10757951488; temporary-memory bytes available 31079641088.
Source base before opening metadata: 5f6f5f886c79ce88b3aa525b8410391de99b6f6d.

## Memory-backed build reservation

The original disk floor assumed compiler output on persistent storage. This
unit now writes compiler output to its exclusive temporary-memory target.
Measured at this change: persistent bytes available 9461886976; temporary-memory
bytes available 31051960320; host MemAvailable 38575600 KiB.

Before each compiler invocation, require at least 4 GiB persistent free space,
8 GiB free on the temporary-memory filesystem and 8 GiB host MemAvailable.
Retain the two-job cap, disabled incremental/debug output and task-owned paths.
The coordinator has retired its idle provider/probe compiler outputs. Persistent
source, logs, exact failed inputs and fixture evidence remain retained. If any
floor fails, stop new compilation and report; do not clean unrelated storage.
This resource adjustment supersedes the earlier 10 GiB persistent floor only for
this memory-backed unit. It changes no acceptance or source gate.

## Absent transaction target qualification

The source worker identified an omitted named refusal. DESIGN's appended
Absent Transaction Command Targets amendment and the shared kernel ESS now
require TransactionNotFound before state/basis/staleness evaluation, without
writes or a fabricated transaction state. Both active trees have identical
shared declarations. This is a contract correction, not executed runtime evidence.

Released compiler output retained under the unit scratch transaction-not-found directory:
- generated: 35; authored: 0; refused: 0; outside: 0.
Validation and compilation also exit successfully. The implementation and later
real-target conformance must execute the new refusal and no-write controls.
