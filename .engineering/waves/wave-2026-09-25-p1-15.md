# Wave p1-15 — platform refresh

Skill: aep-drive:wave 0.9.3. Coordinator: Claude session `claude-lazy-sutton-p1-15`.
Status: **running** (approved 2026-09-25 by the operator). Plan: wave 1 of `<home>/.claude/plans/continue-ekr-epistemic-knowledge-lazy-sutton.md`
(approved 2026-09-25). Base: `main` @ `5984d70`.

Upstream at proposal: ESS 0.32.0 released (tag commit `f4c1bb84`), eventlog 0.4.0 (`70096af8`),
AEP 0.59.3 (0.60.0 not yet released; the planning move needs ≥ 0.59.2).

## Stories

| story | serves | scope | confidence |
|---|---|---|---|
| `story:pin-ess-0-32` | `vision:o2` | pins, conformance suite, task re-checks | medium |
| `story:eventlog-0-4-batched-reads` | `vision:o2` | `ekr-store` `load_history`, eventlog pin, `story_contract.rs` | high |
| `story:planning-store-on-project-3` | `vision:o2` | `.engineering/`, CI AEP pin | high |
| `story:store-open-semantics` | `vision:o5` | `ekr-store` open path, `Runtime`, CLI dispatch, seed | high (files), medium (lock cause) |
| `story:seed-evidence-content-hash` | `vision:o5` | `ekr-kernel` seed refusal, CLI verb and guide | medium |

## Verb output, verbatim

`aep plan artifact waves --kind story --status draft --format json`, reduced by `jq`:

```json
{"waves":[{"wave":1,"ids":["story:eventlog-0-4-batched-reads","story:planning-store-on-project-3","story:seed-evidence-content-hash"]},{"wave":2,"ids":["story:pin-ess-0-32"]},{"wave":3,"ids":["story:store-open-semantics"]}],"collisions":[{"a":"story:eventlog-0-4-batched-reads","b":"story:pin-ess-0-32","path":"Cargo.lock","confidence":"cited"},{"a":"story:eventlog-0-4-batched-reads","b":"story:pin-ess-0-32","path":"Cargo.toml","confidence":"cited"},{"a":"story:eventlog-0-4-batched-reads","b":"story:store-open-semantics","path":"crates/ekr-store/src/eventlog.rs","confidence":"cited"},{"a":"story:pin-ess-0-32","b":"story:planning-store-on-project-3","path":".github/workflows/correctness.yml","confidence":"cited"},{"a":"story:pin-ess-0-32","b":"story:planning-store-on-project-3","path":"README.md","confidence":"cited"},{"a":"story:pin-ess-0-32","b":"story:planning-store-on-project-3","path":"Taskfile.yml","confidence":"cited"},{"a":"story:pin-ess-0-32","b":"story:store-open-semantics","path":"crates/ekr/src/conformance.rs","confidence":"inferred"},{"a":"story:seed-evidence-content-hash","b":"story:store-open-semantics","path":"crates/ekr-kernel/src/commit.rs","confidence":"inferred"},{"a":"story:seed-evidence-content-hash","b":"story:store-open-semantics","path":"crates/ekr-kernel/src/seed.rs","confidence":"cited"},{"a":"story:seed-evidence-content-hash","b":"story:store-open-semantics","path":"crates/ekr/src/cli/mod.rs","confidence":"inferred"},{"a":"story:seed-evidence-content-hash","b":"story:store-open-semantics","path":"crates/ekr/tests/agent_cli.rs","confidence":"inferred"}],"unassessed":[],"cycles":[]}
```

## Proposed order

| round | units (concurrent) | why |
|---|---|---|
| A | U1 `eventlog-0-4-batched-reads`, U2 `seed-evidence-content-hash`, U3 `pin-ess-0-32` | U1 and U3 collide only on `Cargo.toml` (split by line: ESS :29-30 is U3, eventlog :31-33 is U1) and `Cargo.lock` (regenerated by the coordinator at merge). U3 does not touch `correctness.yml`'s AEP lines, `Taskfile.yml`'s `plan-check` or `README.md`'s AEP words |
| B | U4 `store-open-semantics` | collides with U1 (`eventlog.rs`) and U2 (`seed.rs`, `cli/mod.rs`); forks from the integration branch after A merges |
| close | coordinator: `planning-store-on-project-3` | store-wide; migrates `aep.project/1 → /2 → /3` after the wave's last store write, before the PR |

The verb's wave 1 includes the planning move; the coordinator runs it last instead, because it
rewrites the store every other step writes. This disagrees with the verb, and is reported.

## Pre-flight, 2026-09-25 20:56

| check | value | source |
|---|---|---|
| primary | `main` @ `5984d70`, clean, equal to `origin/main` | `git status -sb` |
| worktrees | primary + `wave-p1-15` | `git worktree list` |
| `/` free | 41G | `df -h /` |
| `/dev/shm` free | 32G | `df -h /dev/shm` |
| load average | 5.99 on 20 cores | `uptime` |
| leftover EKR build dirs | none | `ls <cache>/b10x-target` |
| build caps | `nice -n 19`, `CARGO_BUILD_JOBS=4`, `RUST_TEST_THREADS=4`, build dirs in `/dev/shm` | `<home>/.claude/CLAUDE.md` |
| last wave cost | p1-14: 5,068,215 sub-agent tokens, 3 units | wave p1-14 page |

Dispatch types: `aep-drive:implementor`, `aep-drive:adversary` (two passes each),
`general-purpose` for one blind agent trial (U2). Scopers used: `aep-drive:story-scoper` ×5.

## Commits the approval authorises

One commit per unit round, the bot-authored merges into `wave/wave-p1-15`, this page's and the
store's commits, the planning-store migration commits, the bot push, the pull request and its merge
into `main`, and the `gates-policy` baseline commit after it. No tag.
