# Wave p1-14 — conformance and the P1 exit

Skill: aep-drive:wave 0.9.3. Coordinator: Claude session `claude-lazy-sutton-p1-14`.
Status: **running**. Approval: the operator approved the refined plan on 2026-09-23
(`<home>/.claude/plans/continue-ekr-epistemic-knowledge-lazy-sutton.md`), wave 2 of
`<home>/.claude/plans/golden-jingling-kite.md`. Base: `main` @ `d865a82`.

The opening store commit `014c901` (plan review of `72779f9`) was made on this branch before this
page existed; it is part of this wave.

## Units

| unit | artifacts | branch | tree id | build dir | scratch | stage |
|---|---|---|---|---|---|---|
| p1-14-conformance | `story:ess-conformance-kernel`, `task:ess-retained-command-results` | `wave/p1-14-conformance` | `p1-14-conformance` | `<cache>/b10x-target/ekr-p1-14-conformance` → `/dev/shm/ekr-p1-14/p1-14-conformance` | `<cache>/ekr-p1-14/p1-14-conformance` | round 1 red 35/36; kernel.yaml `e128e4e`; round 2 green `0ac4eb7` 36/36 both providers; integration merged in `b7e12b4`; adversary pass 1 |
| p1-14-exit | `story:p1-exit-properties`, `task:canonical-reference-holds-a-node-id-for-every-target` | `wave/p1-14-exit` | `p1-14-exit` | `<cache>/b10x-target/ekr-p1-14-exit` → `/dev/shm/ekr-p1-14/p1-14-exit` | `<cache>/ekr-p1-14/p1-14-exit` | round 1 green `ba0f59d`; pass 1: 3 findings (1 warning); correction round 1 (429 at 16:20, resumed with context, no re-dispatch) |
| p1-14-bindings | the six domain-drift tasks (below) | `wave/p1-14-bindings` | `p1-14-bindings` | `<cache>/b10x-target/ekr-p1-14-bindings` → `/dev/shm/ekr-p1-14/p1-14-bindings` | `<cache>/ekr-p1-14/p1-14-bindings` | round 1 green `4956f27`; pass 1: 6 findings (4 introduced); correction 1 `e6be00b`; pass 2 `9aed936`: 4 notes, held by its own cases; ledger carried 1, new 3, resolved 5; merged |
| integration | — | `wave/wave-p1-14` | `wave-p1-14` | `<cache>/b10x-target/ekr-wave-p1-14` → `/dev/shm/ekr-p1-14/wave-p1-14` | `<cache>/ekr-p1-14` | open |

Domain-drift tasks in p1-14-bindings: `ess-domain-carries-compound-value-types`,
`ess-has-no-byte-string-type`, `graph-domain-carries-validation-state-payloads`,
`graph-properties-keyed-by-string`, `store-snapshot-and-its-id-are-declared-not-implemented`,
`typed-value-canonical-field-is-undecided`.

All serve `vision:o2`. Implementors `aep-drive:implementor`, adversaries `aep-drive:adversary`
(two passes), scopers `aep-drive:story-scoper`.

Left out: `task:sqlite-provider-open-contention` (P4), `task:object-payloads-belong-in-provider-blobs` (P7).

Ownership: every edit under `systems/` is the coordinator's and is copied to every tree (check 3).
`Cargo.lock` and root `Cargo.toml` belong to p1-14-conformance; p1-14-bindings may add a `serde_yaml_ng` dev-dependency to ekr-core, ekr-graph and ekr-store (lock conflict resolved at merge).

## Scoping, 2026-09-23

Eight `aep-drive:story-scoper` runs. `## Scope` written on all eight; typed entries on the story
only, because `aep plan artifact scope` refuses a task ("a task inherits the surface of the story
it decomposes").

| artifact | verdict | unit |
|---|---|---|
| `story:p1-exit-properties` | scope medium; needs the associated-`Id` design | p1-14-exit |
| `task:canonical-reference-holds-a-node-id-for-every-target` | subset of the story | p1-14-exit |
| `task:ess-domain-carries-compound-value-types` | binding case needed (`ekr-ontology/tests/domain_projection.rs`) | p1-14-bindings |
| `task:graph-properties-keyed-by-string` | binding case needed (`ekr-graph/tests/domain_projection.rs`) | p1-14-bindings |
| `task:store-snapshot-and-its-id-are-declared-not-implemented` | remove 2 declarations (coordinator) + declaration-coverage case | p1-14-bindings |
| `task:ess-has-no-byte-string-type` | already held, archive at close | coordinator |
| `task:graph-domain-carries-validation-state-payloads` | already held, archive at close | coordinator |
| `task:typed-value-canonical-field-is-undecided` | already held, archive at close | coordinator |

`aep plan artifact waves --kind story --status draft --format json`, reduced by `jq`:

```json
{"waves":[{"wave":1,"ids":["story:ess-conformance-kernel","story:p1-exit-properties"]}],"collisions":[],"unassessed":[],"cycles":[]}
```

`--kind task`: waves `[]`, collisions `[]`, unassessed: all 9 draft tasks (no task can carry entries).

**Collision the verb cannot see.** p1-14-exit and p1-14-bindings both edit
`crates/ekr-graph/tests/domain_projection.rs` (exit: FUSIONS `"Edge(EdgeId)"` tokens at :533,
:539, :605; bindings: a new case). Split by symbol: exit edits only the FUSIONS table entries;
bindings adds one new `#[test]` fn at the end of the file and owns the doc block
`crates/ekr-graph/src/node.rs:1-40`. `git merge-tree --write-tree` dry run before the first of the
two merges.

## Pre-flight, 2026-09-23

| check | value | source |
|---|---|---|
| primary | `main` @ `d865a82`, clean | `git status` |
| worktrees before | primary + `wave-p1-14` (this wave, no live lease) | `git worktree list`, `worktree inspect` |
| `/` free | 80G (91% used) | `df -h /` |
| `/dev/shm` free | 32G | `df -h /dev/shm` |
| leftover build dirs | `<cache>/b10x-target/ekr-wave-p1-14` 0 B | `du -sh` |
| leftover scratch | `<cache>/ekr-p1-13` 11M (wave p1-13) | `du -sh` |
| model budget | N = 3 (plan) | operator plan |
| p1-13 sub-agent cost | 1,721,934 tokens, 2 units | wave p1-13 page |

## Commits the approval authorises

One commit per unit round, the bot-authored merge of each unit into `wave/wave-p1-14`, this page's
and the store's commits (including `014c901`), the bot push, the pull request and its merge into
`main`. No tag.

## Disk during the wave

| time | `/` free | note | source |
|---|---|---|---|
| 13:20 | 79G | pre-flight | `df -h /` |
| 14:00 | 31G | `<cache>/b10x-target/{ess 28G, aep 13G, service-sdk-d1 10G}` written 13:54-13:59 by other sessions; this wave builds in `/dev/shm` (16.4G) and scratch is 659M | `du -sh` |
