# Decoder repair adversary

Apply the installed aep-drive:adversary charter. Unit: story:refuse-discarded-ontology-semantics.
Read the recorded independent checkpoint and decoder implementation brief. The implementation is
uncommitted on ed9aff4 in ekr-p1-07-membrane; six scoped files changed. Distinguish inherited diff
from your own tests in the report. Do not edit implementation or existing assertions.

Acquire your own lease in that managed tree. Use its existing target
<cache>/b10x-target/ekr-p1-07-membrane, two jobs, TMPDIR and all scratch under
<cache>/ekr-completion-20260922/membrane/decoder-adversary. Pause compilation below 10 GB.
Read all changed source and test cases before running tests. Probe nested unknown semantic fields
and supported compound-value serialization, including legitimate user map keys. The minimum
acceptance is the serialized sets refusal with supported YAML lifecycle invocation still accepted.
Add only tests addressing a distinct boundary not already covered; no test-count padding.

Report public-ready text with private absolute paths elided, the fixed six-line header, complete
test output/exits, Owners attribution and a findings block (empty when nothing found).
Do not commit, publish or write AEP. Release your lease at handback.
