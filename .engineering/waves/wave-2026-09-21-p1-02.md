# Wave 2026-09-21 p1-02 — stable identity and content hashing

Skill version 0.9.3. Coordinator: the Claude session that ran p1-01. Operator instruction, in
session: "good do it" against a description naming wave 2 and the same loop (2026-09-21).
Interactive run, pre-approved: the stage-1 stop is waived, the page is not.

N = 1. `aep plan artifact waves --kind story --status draft` puts exactly one story in wave 1 of
the remaining eight; every later P1 story `depends_on` it, transitively.

## Commits this wave authorises

| # | commit | branch |
|---|---|---|
| 1 | opening store commit: this page, the epic to `active`, the story to `active` with its `serves` edge | `wave/p1-02` |
| 2 | one unit commit | `impl/kernel-identity-and-hashing` |
| 3 | `Merge impl/kernel-identity-and-hashing: …` | `wave/p1-02` |
| 4 | closing store commit: gate evidence, story `implemented`, scope confirmed | `wave/p1-02` |
| 5 | the merge of the pull request that brings `wave/p1-02` into `main` | `main` |

Nothing else: no push beyond the two wave branches, no tag, no release.

**Commit 5 is a pull request, not a push.** Since p1-01 the repository carries the org-standard
rulesets, and `Required shared and repository gates` makes `common / Security and privacy` a
required check on `main` with no bypass actor. A direct push to `main` is refused; the branch goes
up, the App opens the pull request, the check runs on it, the App merges it.

## Selection

`aep plan artifact waves --kind story --status draft`, protocol 0.55.0:

```text
wave 1
  story:kernel-identity-and-hashing (inferred)
wave 2
  story:ontology-types-and-values (inferred)
wave 3
  story:graph-model-and-assertions (inferred)
wave 4
  story:eventlog-store (inferred)
  story:transaction-and-validators (inferred)
wave 5
  story:commit-and-revision-lineage (inferred)
wave 6
  story:seed-and-explain (inferred)
wave 7
  story:ekr-cli (inferred)
wave 8
  story:ess-conformance-kernel (inferred)
8 wave(s), 0 collision(s), 0 unassessed
```

Selection path: the verb. No collisions and nothing unassessed, so no `story-scoper` was
dispatched — the unassessed set the skill says to scope is empty.

**Scope stays `inferred`, and that is not laziness.** Three of the story's four surfaces —
`crates/ekr-core/src/{identity,hash,canonical}.rs` — do not exist yet; the story creates them.
Only `crates/ekr-core/src/lib.rs` is in the tree. A scoper cannot cite a file that is not there.
The implementor confirms the scope in its report and the closing step writes the confirmation back.

Blast radius: one package, `ekr-core`, which nothing yet depends on in code.

## Unit

| | |
|---|---|
| story | `story:kernel-identity-and-hashing` |
| acceptance | a property test renames a node's `canonical_name` a thousand times and its `NodeId` never changes |
| branch | `impl/kernel-identity-and-hashing`, forked from `wave/p1-02` |
| worktree | managed id `ekr-impl-kernel-identity`, path `$HOME/.local/state/worktree/trees/b10x/epistemic-knowledge-runtime/ekr-impl-kernel-identity` |
| build dir | `$HOME/.cache/b10x-target/epistemic-knowledge-runtime` — shared per `AGENTS.md` § The gate. N = 1, so no two trees build at once |
| scratch | `$HOME/.cache/ekr-wave-p1-02/unit-scratch` |
| stage | proposed |
| head | — |
| subagent_type | `aep-drive:implementor`, then `aep-drive:adversary`, both on Opus |

Integration checkout: `wave/p1-02`, forked from `main` at `4d2cc1d`; managed id `ekr-wave-p1-02`,
coordinator lease `ekr-coordinator-p1-02`.

## Pre-flight

| check | reading |
|---|---|
| primary on `main`, clean, synced | yes; `4d2cc1d` == `origin/main`; 0 porcelain after the epic move was moved onto this branch |
| `git worktree list` | 1 entry before this wave, the primary |
| free disk | 62G on `/` (floor 10G), up from 28G during p1-01 as processes outside this repository released space |
| build directory | 788M, shared, carried over from p1-01 |
| one measured build | `task check` warm: 1.1s, exit 0 |
| build cache | `sccache` at `/usr/bin/sccache`, `RUSTC_WRAPPER` unset — accepted at N = 1 |
| model budget | not stated by the operator; N = 1, so the skill's default of 4 does not bind |
| `AGENTS.md` | read; its invariants go into the unit brief unchanged |
| p1-01 cost, for pacing | 481,344 sub-agent tokens, 171 tool uses, ~28 minutes, for six empty crates and 12 contract cases |

## Gate

`task check` (fmt, clippy `-D warnings`, test, doc, `ess specify validate`,
`aep plan artifact validate`), run once on `wave/p1-02` after the merge, one exit status per step.

## Release

Not part of this wave. 0.1.0 is P7's exit, through the bot route, after a person decides.
