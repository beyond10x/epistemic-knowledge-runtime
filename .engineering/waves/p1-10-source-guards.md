# P1-10 — source guards

N=1, story:source-guard-debt, under the operator's approved completion plan.
The bounded unit groups already recorded guard tasks and introduces no new entity.
The full selection is p1-10-selection.json; inspected surfaces and collisions are
in p1-guard-debt-scope.md. Persisted-contract source activation follows this unit
because its graph/store projection test paths overlap. Upstream Eventlog atomic
source can continue independently in its separate repository.

Start after the integrated original-format freeze passes its corrected full gate.
Managed id: ekr-p1-10-guards-20260922. Branch: codex/ekr-p1-10-guards-20260922.
Target: <cache>/b10x-target/ekr-p1-10-guards-20260922.
Scratch: <cache>/ekr-completion-20260922/guard-debt/implementation.
Use separate targets, two Cargo jobs and the existing measured disk floor.
Every actor holds and releases its own worktree lease. Root owns commits, planning,
AGENTS/README policy prose and integration; the implementor owns scoped Rust tests
and xtask only. Any required additional path is reported before widening scope.

Coordinator checks before dispatch: the runtime-path rule applies to every new
guard here; existing quoted comments do not count as executable macros. No systems
declaration changes occur in this unit. Behavioral guards remain meaningful: source
mutations become refusals, and the negative semantic temporal witnesses remain
valid tests. A public-type inventory must not be satisfied with no-op name mentions.
README membership is measured from Cargo, with xtask clearly distinguished from
product crates. Current source behavior remains honestly described as incomplete.

One compiled guard is also run against a distinguishing source mutation in another
managed checkout. It must read the runtime checkout, and the original bytes are
restored before review. This bounded experiment is the sole deliberate artifact
reuse; normal concurrent builds remain isolated. Preserve public-surface, temporal
and README mutation failures, then independent adversarial review and the complete
integration gate. All source/API assertions here are acceptance, unexecuted at opening.

The host lacks plugin subagent_type dispatch; workers load the installed aep-drive
implementor and adversary charters explicitly. No unavailable cost counter is claimed.
