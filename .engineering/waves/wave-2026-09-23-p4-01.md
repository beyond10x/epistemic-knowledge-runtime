# Wave p4-01 — the `ekr` binary, discoverable by an agent

Skill: aep-drive:wave 0.9.3. Coordinator: Claude session `claude-lazy-sutton-p1-14`.
Status: **closed** (see Close). Approval: operator goal of 2026-09-23 — "ekr usable for an AI agent like claude
code via CLI with operations for manipulation exposed via CLI in an discoverable way" — and the
operator's approval of the plan steps 1–6 the same evening. N = 1: one package, `crates/ekr`.

Base: `wave/wave-p1-14` @ `5eb4ff1` (pull request 16, not yet merged), so this wave's pull request
stacks on it. The unit started earlier on `658c59f`, the p1-14 conformance head.

## Units

| unit | story | branch | tree id | build dir | scratch | stage |
|---|---|---|---|---|---|---|
| agent-cli | `story:agent-discoverable-cli` | `wave/p4-01-agent-cli` | `p4-01-agent-cli` | `/dev/shm/ekr-p4-01/agent-cli` | `<cache>/ekr-p4-01/agent-cli` | green `8303d30`; two adversary passes; last correction verified by the coordinator |
| integration | — | `wave/wave-p4-01` | `wave-p4-01` | `<cache>/b10x-target/ekr-wave-p4-01` → `/dev/shm/ekr-p4-01/wave-p4-01` | `<cache>/ekr-p4-01` | open |

Implementor `aep-drive:implementor`, adversary `aep-drive:adversary` (two passes), blind trials
`general-purpose` with only the binary.

## Rounds

| round | commit | result |
|---|---|---|
| 1 | `e74a7fb` | 12 new cases; `-p ekr` 128 → 140 |
| adversary pass 1 | — | 6 findings (1 blocker: four kinds the P1 kernel refuses were documented as writable), 4 red cases |
| blind trial 1 | — | PASS, 24 invocations; 6 friction points (number-array bytes, relation vs edge, assessment, a stale example id, unlabelled evidence, `--valid-at`) |
| correction 1 | `0551000` | all six findings and five friction points; `-p ekr` 146 passed |
| adversary pass 2 | — | 6 findings (1 warning: evidence payloads promised but never printed), 4 red cases |
| last correction | `8303d30` | explain prints evidence payloads; guide wording; `EKR_BACKEND` case; coordinator run: 154 passed, 0 failed, 22 targets |
| blind trial 2 | — | PASS 6 of 6 on sqlite (new node, relation, property, retraction, evidence text) |

Trial reports: `<cache>/ekr-p4-01/trial-1.md`, `<cache>/ekr-p4-01/trial-2.md`.

## Commits the approval authorises

One commit per unit round, the bot-authored merge into `wave/wave-p4-01`, this page's and the
store's commits, the bot push, the pull request and its merge into `main`. The operator also
approved tag `0.0.2` for the P1 exit (after pull request 16).

## Close, 2026-09-25

Gate on `wave/wave-p4-01` @ `c3040ad` under `nice -n 19`, `CARGO_BUILD_JOBS=4`, `RUST_TEST_THREADS=4`: fmt-check 0, clippy 0, test 0 (798 passed, 0 failed, 135 targets), doc-check 0, vendor-check 0, spec-check 0 (ESS 0.29.0), conform-check 0, plan-check 0 (AEP 0.57.0).

`story:agent-discoverable-cli` implemented on that gate. Follow-ups filed: `task:no-store-at-a-mistyped-path`, `task:agent-cannot-add-evidence`, `task:store-reads-rehash-the-whole-log` (a Fable performance review measured the file provider quadratic in revisions; eventlog 0.4.0 addresses it).

The first gate attempt on 2026-09-23 ran uncapped, drove the load average to 33 on 20 cores and was stopped by the operator; every later build ran capped.
