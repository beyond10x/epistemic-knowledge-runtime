# Wave 2026-09-21 p1-04 — the graph model

Skill version 0.9.3. Coordinator: the session that ran p1-01, p1-02 and p1-03. Operator approved
the plan *The next three waves of P1* (2026-09-21), which names this wave as the second of the
three. Interactive run, pre-approved: the stage-1 stop is waived, the page is not.

N = 1. `aep plan artifact waves --kind story --status draft`, read after p1-03 merged, puts exactly
one story in wave 1 of the remaining six. Both p1-05 units consume `Root` and `RevisionEvent`,
which this wave defines, so it cannot run beside them.

## Commits this wave authorises

| # | commit | branch |
|---|---|---|
| 1 | opening store commit: this page, the coordinator work below, the story to `active` with its `serves` edge | `wave/p1-04` |
| 2 | one unit commit | `impl/graph-model-and-assertions` |
| 3 | closing store commit: gate evidence, story `implemented`, scope confirmed | `wave/p1-04` |
| 4 | the merge of the pull request that brings `wave/p1-04` into `main` | `main` |

Nothing else: no push beyond the two wave branches, no tag, no release. The branch is linear.
`b10x-gates bot` has no merge verb, so the only merge is the pull request's.

## Coordinator work, in the opening commit

### 1. `Timestamp` is settled before dispatch, and not where the plan put it

`architecture-decision-record:0004-timestamp-in-ekr-core`, accepted: `Timestamp` is a newtype in
**`ekr-core`** over `i64` milliseconds since the Unix epoch, structural under `Canonical`, with a
decimal text form.

The plan said `ekr-graph` would define it. Reading the specification before dispatch overturned
that. `systems/ekr/domains/ontology.yaml:100-101` declares `SchemaVersion.created_at` of type
`Timestamp`, and `ekr-ontology` does not depend on `ekr-graph` — the order is
`ekr-core <- ekr-ontology <- ekr-graph <- ekr-store <- ekr-kernel`. A `Timestamp` in `ekr-graph`
would leave one ESS scalar carried by two unrelated Rust types, which is the drift
`crates/ekr-ontology/tests/domain_projection.rs` exists to catch. Four of the five domains declare
a `Timestamp` field, so the type belongs at the bottom of the stack.

The consequence is a scope extension the story did not have: `ekr-ontology` carries a bare `i64` at
three sites today (`value.rs:208`, `value.rs:149`, `schema.rs:31`) and adopts the newtype. Written
into the story's typed scope and its `## Scope` section before dispatch.

### 2. The acceptance was restated after adversary pass 1

It read "answers `active()` with Bob", and that was wrong for a reason no pre-dispatch reading
caught. `active()` as built is the same read as `valid_at(i64::MAX)`, measured over 56 records of
every shape, so its doc's "holds true now" means the end of representable time. A fixed-term fact
true today is excluded because its end is known; an announced successor whose tenure has not begun
is included.

There is no clock and P1 declares no time crate, so a clock-free current-world read cannot exist.
`valid_at(t)` becomes the only read — the current-world query at the caller's now, the historical
query at any other instant — and `active()` is deleted rather than renamed. The acceptance now
reads: the § 65 fixture answers `valid_at(t)` with Bob for every `t` at or after the handover, with
Alice for every `t` before it, and the handover instant belongs to exactly one of them.

**This is the third wave running where a defect visible in the story before dispatch cost an
adversary round.** The plan front-loaded two acceptance restatements and missed this one, because
it checked whether the statement could fail and not whether the method it names can mean what it
says.

### 3. The mechanism the p1-03 decision deferred lands here

`task:canonical-newtype-discriminant` settled that sum types carry a variant tag and newtypes do
not. `crates/ekr-core/src/canonical.rs` states it as a contract and names `RevisionEvent` as the
first sum type that needs the writer. This wave adds `tag::VARIANT` and `Encoder::variant`, and the
task closes when they land with a caller.

## Selection

`aep plan artifact waves --kind story --status draft`, protocol 0.55.0, read from `main` at
`bd6b312`:

```text
wave 1
  story:graph-model-and-assertions (inferred)
wave 2
  story:eventlog-store (inferred)
  story:transaction-and-validators (inferred)
wave 3
  story:commit-and-revision-lineage (inferred)
wave 4
  story:seed-and-explain (inferred)
wave 5
  story:ekr-cli (inferred)
wave 6
  story:ess-conformance-kernel (inferred)
6 wave(s), 0 collision(s), 0 unassessed
```

Selection path: the verb. No collisions, nothing unassessed, so no `story-scoper` was dispatched.
Scope is `inferred` for the `ekr-graph` files, which do not exist yet, and `cited` for the six
`ekr-core` and `ekr-ontology` files the coordinator read before extending it.

Blast radius: `ekr-graph`, plus two files each in `ekr-core` and `ekr-ontology` that the ADR pulls
in. Nothing depends on `ekr-graph` in code today.

## Unit

| | |
|---|---|
| story | `story:graph-model-and-assertions` |
| acceptance | a `GraphSnapshot` over the § 65 fixture answers `active()` with Bob |
| branch | `impl/graph-model-and-assertions`, forked from `wave/p1-04` |
| worktree | managed id `ekr-impl-graph`, path `$HOME/.local/state/worktree/trees/b10x/epistemic-knowledge-runtime/ekr-impl-graph` |
| build dir | `$HOME/.cache/b10x-target/epistemic-knowledge-runtime` — shared, and N = 1 so no two trees build at once |
| scratch | `$HOME/.cache/ekr-wave-p1-04/unit-scratch` |
| stage | **closed, green.** implementor (Opus, 269k tokens; 146→188 cases, gate 0) → adversary pass 1 red (Opus, 186k; 192 cases, 4 red, 8 findings, 1 blocker) → correction 1 green (Opus, 358k; 193 cases, gate 0) → adversary pass 2 red (Opus, 283k; 196 cases, 3 red, 5 new findings, 0 carried, 1 blocker) → correction 2 green (Opus, 427k; 199 cases, gate 0) → coordinator verification, below |
| head | `bf03b76` |
| subagent_type | `aep-drive:implementor`, then `aep-drive:adversary`, both on Opus |

Integration checkout: `wave/p1-04`, forked from `main` at `bd6b312`; managed id `ekr-wave-p1-04`,
coordinator lease `ekr-coordinator-p1-04`.

## Pre-flight

| check | reading |
|---|---|
| primary on `main`, clean, synced | yes; `bd6b312` == `origin/main`; 0 porcelain |
| `git worktree list` | 1 entry before this wave, the primary |
| free disk | 53G on `/` (floor 10G) |
| build directory | 2.0G, shared, carried from p1-03 |
| one measured gate in the new tree | `task check` 5s first run, 1s warm, exit 0 both |
| gates adoption baseline | `bd6b3128ca78582d5993069759aba70b2ee50f8d`, equal to `main`, advanced after p1-03 merged. The edit to `gates-policy/policy.json` is uncommitted — that repository is outside the wave's commit grant |
| `AGENTS.md` | read; its invariants go into the unit brief unchanged |
| cost so far | p1-01 481k sub-agent tokens, p1-02 867k, p1-03 roughly 600k |

## Gate

`task check` (fmt, clippy `-D warnings`, test, doc, `ess specify validate`,
`aep plan artifact validate`), run once on `wave/p1-04` after the unit merges, one exit status per
step.

## Coordinator verification of correction 2

No third attack. What was checked, and how:

| claim | how it was read |
|---|---|
| no assertion dropped | 199 cases, `0 ignored` in every `test result` line, all seven adversary case names present in the tree |
| no case relaxed to pass | the four re-aimed pass-1 cases and the three pass-2 cases keep their original names. The reviewer judged its own re-aims in pass 2: two stronger than the originals, one redirected because the state it described became unrepresentable, one weakened in a way it raised as its own finding |
| the restated acceptance is measured | `TemporalRange::contains` at `crates/ekr-graph/src/assertion.rs:107` was mutated from `at < to` to `at <= to`, closing the upper bound of every range. Four cases went red, including `valid_at_answers_alice_before_the_handover_and_bob_at_or_after_it` and `the_handover_instant_belongs_to_exactly_one_of_them`. Restored, 199 green |
| the implementor's own probes | four, each reddening exactly its own case: a field renamed to one another entity carries, both inversion refusals removed, the banned filter rebuilt in another module, a unit variant inserted mid-list |

## A privacy violation the review passes did not catch

`b10x-gates scan-text` over the staged files refused
`crates/ekr-graph/tests/evidence_and_observations.rs` at lines 65 and 68: a real personal
identifier had been used as an `EvidenceSource::HumanStatement` fixture. It was written in the
first implementor round and survived two adversary passes, two correction rounds and six gate runs,
because `task check` does not scan text and neither brief asked anyone to look.

Replaced with `h.witness` by the coordinator before anything was committed, and the whole tree
rescanned: 188 files, no private rule. `AGENTS.md` already says fixtures use the runtime's own
vocabulary; nothing mechanical enforced it until the push-time scan.

## What the coordinator wrote, beyond the three opening items

- `systems/ekr/domains/graph.yaml` lines 50-51 claimed a payload carrier for four validation states
  and there is one, `superseded_by`. Corrected. This is the same error as the `ontology.yaml`
  comment in wave p1-03, found the same way, by the implementor rather than by the coordinator.
- Six tasks filed: `graph-domain-carries-validation-state-payloads`, `graph-properties-keyed-by-string`,
  `trybuild-stderr-is-toolchain-pinned`, `assertion-retraction-erases-its-acceptance` (blocking
  `story:commit-and-revision-lineage`), `open-ended-read-ban-binds-one-crate`.
- ADR 0004 corrected from three `ekr-ontology` adoption sites to two.
- `task:canonical-newtype-discriminant` closed out: the writer landed with its first caller. Its
  body said `RevisionEvent` has seven variants; it has six, and that is corrected.
- The story's scope written back: 37 paths, every one `cited`, and the `## Scope` section names each
  test file.

## Gate, on `wave/p1-04` at `bf03b76`

| step | exit |
|---|---|
| `fmt-check` | 0 |
| `clippy` | 0 |
| `test` | 0 — 199 cases, 0 failed, 0 ignored |
| `doc-check` | 0 |
| `spec-check` | 0 |
| `plan-check` | 0 |

`b10x-gates scan-text` over every tracked file: `188 file(s) carry no private rule`, exit 0.

## Release

Not part of this wave. 0.1.0 is P7's exit, through the bot route, after a person decides.
