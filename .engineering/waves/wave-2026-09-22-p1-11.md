# Wave p1-11 — finish the in-flight durable writer

Skill: aep-drive:wave 0.9.3. Coordinator: Claude session `claude-579ad7f9-coordinator`.
Status: **approved and running** (revision 3).

## Execution record (revision 3)

Operator instruction, 2026-09-22: "cleanup the mess, fix compile errors, make mergeable", "upgrade
all dependencies to latest versions", and the session goal "new release cut". That instruction
approves this wave and names its outcome: `b208e2f` plus the fixes below, green on the full gate,
merged to `main` and released. It supersedes revision 2's "U1 only, nothing dispatched".

Scope actually worked, all in the integration tree `ekr-completion-20260922` at base `b208e2f`:

| work | owner | state |
|---|---|---|
| `ekr-graph` test migration: 5 targets, 1 doctest, 1 compile-fail snapshot | coordinator | green |
| `cargo doc -D warnings` (was exit 101 at `crates/ekr-store/src/lib.rs:34` and the kernel's stale module list) | coordinator | exit 0 |
| `crates/ekr/tests/story_contract.rs` (dependency table, seed-admission guard) and `story:workspace-crate-skeleton` revision 19 | coordinator | 11/11 |
| `AGENTS.md` invariant 1: named `ValidatedSeed`, deleted in `edf4799` | coordinator | rewritten |
| `ekr-store` tests: fold/authority (4 files) | `aep-drive:implementor` unit `store-fold` | running |
| `ekr-store` tests: runtime context (2 files) | `aep-drive:implementor` unit `store-runtime` | green: 11 of 11 original cases migrated, 6+5 pass; 200494 tokens, 66 tool uses, 672788 ms |
| `ekr-store` tests: providers/objects (4 files) | `aep-drive:implementor` unit `store-providers` | running |
| `crates/ekr/tests/public_surface.rs`: 23 exported items no case uses | coordinator | after store units |
| CI pins: rust-toolchain `02cb101`, Gates shared workflow `d532e72`, AEP `0.57.0` (`ed0f60e`), go-task `v3.53.1` | coordinator | done |
| `sha2` 0.10 → 0.11, `cargo update` | coordinator | after store units |

Brief for the three units: `<cache>/ekr-completion-20260922/claude-gate/store-migration-brief.md`.

**Deviation from the skill: one tree, one build directory, three units.** The three units edit
disjoint test files only (no `src/`, manifest, lock or store writes), so they share the
integration tree and its build directory `<cache>/b10x-target/ekr-completion-20260922`
(→ `/dev/shm/…/completion`). Cargo's lock serialises their builds. AGENTS.md's one-directory-per-tree
rule is about two *trees*; this is one tree. Per-unit worktrees were not made because `/` had 7.2G
free at 20:30 UTC.

**Design rulings applied to tests, not narrowed:**

- `crates/ekr-graph/tests/snapshot_reads.rs`: amendment 88 makes a superseded assertion answerable
  inside its closed valid interval. The case that excluded a superseded record at every `t` was
  rewritten to the retraction alone, and a new case holds the supersession interval.
- `crates/ekr-graph/tests/adversary_snapshot_and_assertion.rs`: `is_current()` has three conjuncts
  since lifecycle is stored; the case now crosses 5 assessments × 3 lifecycles × 2 transaction
  times = 30 records, exactly 1 current.
- `crates/ekr-graph/tests/canonical_value_and_assertion.rs`: the assertion mutation table is 32 rows
  (9 assessment, 7 lifecycle); node properties gain two multi-value rows.

**Release plan.** EKR has no release workflow and no tag. `docs/roadmap.md` P7 reserves `0.1.0` for
cut-over, so this cut is source release `0.0.1`: workspace version bump, bot-authored annotated
tag, GitHub Release through `b10x-gates api`, after the PR's required checks pass on the merge.

Revision 2, on the operator's instruction: integrate what is finished and finish what is in
flight before any new work. U2 (read/CLI) was never started — its tree carries no commit of its
own — so it leaves this page and opens the next wave. This page now carries U1 only.

## Why this is a resumption, not a new wave

The Codex coordinator of `COMPLETION-2026-09-22.md` stopped on 2026-09-22 at 13:52 UTC on a
provider usage limit, mid-turn. Its last commit is `b208e2f` (13:50 UTC), "Integrate incomplete
durable application checkpoint for dependent read and CLI work". At takeover every tree below had
0 tracked, 0 untracked and 0 ignored changes and 0 live leases (`worktree inspect`). No unit is
re-dispatched from scratch: each resumes on its branch head with a brief naming what is already
there. The pre-flight refusal "a previous wave's trees are still there" does not apply as written:
these are this wave's trees, reused.

`b208e2f` is 26 commits ahead of `origin/main` (`165a577`) and exists on no remote.

## Units

| unit | stories | branch @ head | tree id | build dir | scratch |
|---|---|---|---|---|---|
| U1 durable | `story:version-persisted-contracts`, `story:commit-and-revision-lineage` | `codex/ekr-durable-activation-20260922` @ `b208e2f` | `ekr-durable-activation-20260922` | `<cache>/b10x-target/ekr-durable-activation-20260922` → `/dev/shm/…/durable-activation` | `<cache>/ekr-completion-20260922/durable-activation` |
| U2 read/CLI | `story:seed-and-explain`, `story:ekr-cli` | `codex/ekr-cli-host-input-20260922` @ `b208e2f` | `ekr-cli-host-input-20260922` | `<cache>/b10x-target/ekr-cli-host-input-20260922` → `/dev/shm/…/cli-host-input` | `<cache>/ekr-completion-20260922/read-cli` |
| integration | — | `codex/ekr-completion-20260922` @ `b208e2f` | `ekr-completion-20260922` | `<cache>/b10x-target/ekr-completion-20260922` → `/dev/shm/…/completion` | `<cache>/ekr-completion-20260922` |
| review (read-only) | — | detached @ `b208e2f` | `ekr-publication-review-20260922` | `<cache>/b10x-target/epistemic-knowledge-runtime` (independent reviewer) | reviewer's own `mktemp -d` |

All serve `vision:o2`.

**U1 brief:** `p1-durable-activation-brief.md` plus items 1–3 of
`.engineering/reviews/p1-durable-application-remaining.md`, plus every finding of the independent
review of `b208e2f` whose origin is U1's surface. Child tasks that block
`story:commit-and-revision-lineage` stay inside U1: `assertion-retraction-erases-its-acceptance`,
`object-payloads-belong-in-provider-blobs`, `two-of-the-five-revision-sub-roots-are-placeholders`,
`two-revision-events-have-no-discriminator`, `validation-hash-uses-payload-domain`.

**Independent review of `b208e2f`** (`<cache>/ekr-review-b208e2f-fable.md`, 14 findings: 3 blocker,
2 high, 5 medium, 4 low/note). Routed to U1: B1 16 test targets in `ekr-graph`/`ekr-store` do not
compile (180 errors, removed `append`/`admit_seed`/`ValidationState`), matching remaining item 2;
B2 3 guard tests fail in `crates/ekr/tests/story_contract.rs` and `public_surface.rs`; B3
`cargo doc -D warnings` exits 101 at `crates/ekr-kernel/src/lib.rs:16`; H1 invariant 1's named
cases cannot run; H2 no cross-process case for a pending unpublished ordinary preparation, matching
remaining item 1. Coordinator-owned: three draft tasks whose code this range fixes
(`two-of-the-five-revision-sub-roots-are-placeholders`, `validation-hash-uses-payload-domain`,
`two-revision-events-have-no-discriminator`) need evidence and moves at close.

**U2 brief:** `p1-read-cli-implementation.md` unchanged in ownership. The two tasks that block
`story:ekr-cli` are folded into U2's acceptance, a coordinator judgement:
`task:the-proposer-field-is-unauthenticated` names `story:ekr-cli` as the place its binding
belongs (its own body); `task:conformance-command-input-contract` specifies the shared Seed/Propose
document handler the CLI implements. `task:strict-cli-host-input` is already integrated and awaits
the gate.

**Shared file:** `crates/ekr-kernel/src/lib.rs`. U2 owns only the `mod explain;` line and its
`pub use explain::{…};` block; U1 owns the rest. `git merge-tree --write-tree` dry run before the
first unit merges.

**Left out:** `story:ess-conformance-kernel`, blocked by `task:ess-retained-command-results`
(active) and consuming U2's finished CLI. Next wave.

**Deferred by revision 2:** U2 as described above, and its tree `ekr-cli-host-input-20260922`
stays as it is. The U2 rows remain above as the input for the next wave's proposal.

## What is already integrated, and what is not

| work | where it is | source |
|---|---|---|
| EKR PRs #7–#12 | merged to `main` | `gh pr list` |
| ESS PRs #59–#61, release 0.29.0 | merged / released | `gh pr list`, `COMPLETION-2026-09-22.md` |
| Eventlog PRs #15, #17 | merged | same |
| Gates PRs #16, #17; gates-policy adoption branch | merged | same, `git merge-base --is-ancestor` |
| 26 commits `165a577..b208e2f` | local only, no remote | `git branch -r --contains b208e2f` |

The 26 commits cannot merge to `main` yet on their own record:
`p1-durable-application-remaining.md` item 2 says old graph and store fixtures still name replaced
APIs and "no complete workspace compilation or gate is claimed". That is the record's claim; the
independent review of `b208e2f` is measuring it. U1 finishing items 1–3 is what makes them
mergeable.

## Selection

Path: `aep plan artifact waves` (protocol 0.55.0), run in the integration tree at `b208e2f`. The verb
places the two active stories in consecutive waves because of 16 collisions, and the three draft
stories in three waves because of 5 collisions. U1 pairs the two active stories as the Codex
coordinator did. U2 pairs `seed-and-explain` with `ekr-cli` under the partition in
`p1-read-cli-implementation.md`. The `systems/ekr/domains/kernel.yaml` collisions are
coordinator-owned (COORDINATOR check 3), so no unit writes that file. The verb and this selection
disagree, and this paragraph is the report of it. Verb output, verbatim, is appended below.

## Pre-flight, measured 2026-09-22 ~20:15 UTC

| check | value | source |
|---|---|---|
| primary checkout | `main` @ `165a577`, clean, equal to `origin/main` | `git status -sb` |
| `/` free | 11G of 848G (99% used); floor 10G | `df -h /` |
| `/dev/shm` free | 28G of 32G | `df -h /dev/shm` |
| `/tmp` | 25G used against the 25G per-user quota; 18G is another session's `ess-conformance-core-20260921` | `du -sh /tmp/*` |
| MemAvailable | 38337508 kB | `/proc/meminfo` |
| existing build dirs | completion 2.3G, durable-activation 1.1G, cli-host-input 368M, publication-review 0 | `du -sh /dev/shm/ekr-build-cache-20260922-*/*` |
| one measured full build | not measured | — |
| model budget | not stated by the operator | — |

## Dispatch plan

- `aep-drive:implementor` for U1, `aep-drive:adversary` after green, two passes max.
- Dispatch waits for both independent reviews (`b208e2f` and `main` @ `165a577`), so U1's brief
  carries their findings.
- Measured at 20:30 UTC: `/` 7.2G free, below the 10G floor, while the first review builds there.
- Build dirs stay on `/dev/shm`, one per tree. Before each build: `/` ≥ 4 GiB, `/dev/shm` ≥ 8 GiB,
  MemAvailable ≥ 8 GiB (floors from `p1-durable-activation-20260922.md`).

## Commits approval authorises

Revision 3: the fix-up commits on `codex/ekr-completion-20260922`, one closing planning-store
commit, the bot push of that branch, the pull request and its merge into `main`, the `0.0.1` version
commit, tag and GitHub Release. Nothing else — no later wave, no other repository.

## Verb output, verbatim

### `aep plan artifact waves --kind story --status active --format json`, reduced by `jq` to ids, scope counts, collisions and unassessed

```json
{
  "waves": [
    {
      "wave": 1,
      "ids": [
        {
          "id": "story:version-persisted-contracts",
          "scope_entries": 56,
          "inferred": true
        }
      ]
    },
    {
      "wave": 2,
      "ids": [
        {
          "id": "story:commit-and-revision-lineage",
          "scope_entries": 28,
          "inferred": true
        }
      ]
    }
  ],
  "collisions": [
    {
      "a": "story:commit-and-revision-lineage",
      "b": "story:version-persisted-contracts",
      "path": "Cargo.lock",
      "confidence": "cited"
    },
    {
      "a": "story:commit-and-revision-lineage",
      "b": "story:version-persisted-contracts",
      "path": "Cargo.toml",
      "confidence": "cited"
    },
    {
      "a": "story:commit-and-revision-lineage",
      "b": "story:version-persisted-contracts",
      "path": "crates/ekr-core/src/lib.rs",
      "confidence": "inferred"
    },
    {
      "a": "story:commit-and-revision-lineage",
      "b": "story:version-persisted-contracts",
      "path": "crates/ekr-graph/src",
      "confidence": "cited"
    },
    {
      "a": "story:commit-and-revision-lineage",
      "b": "story:version-persisted-contracts",
      "path": "crates/ekr-graph/tests",
      "confidence": "cited"
    },
    {
      "a": "story:commit-and-revision-lineage",
      "b": "story:version-persisted-contracts",
      "path": "crates/ekr-kernel/Cargo.toml",
      "confidence": "cited"
    },
    {
      "a": "story:commit-and-revision-lineage",
      "b": "story:version-persisted-contracts",
      "path": "crates/ekr-kernel/src",
      "confidence": "cited"
    },
    {
      "a": "story:commit-and-revision-lineage",
      "b": "story:version-persisted-contracts",
      "path": "crates/ekr-kernel/src/commit.rs",
      "confidence": "inferred"
    },
    {
      "a": "story:commit-and-revision-lineage",
      "b": "story:version-persisted-contracts",
      "path": "crates/ekr-kernel/src/lib.rs",
      "confidence": "cited"
    },
    {
      "a": "story:commit-and-revision-lineage",
      "b": "story:version-persisted-contracts",
      "path": "crates/ekr-kernel/src/transaction.rs",
      "confidence": "cited"
    },
    {
      "a": "story:commit-and-revision-lineage",
      "b": "story:version-persisted-contracts",
      "path": "crates/ekr-kernel/tests",
      "confidence": "cited"
    },
    {
      "a": "story:commit-and-revision-lineage",
      "b": "story:version-persisted-contracts",
      "path": "crates/ekr-ontology/src",
      "confidence": "cited"
    },
    {
      "a": "story:commit-and-revision-lineage",
      "b": "story:version-persisted-contracts",
      "path": "crates/ekr-ontology/tests",
      "confidence": "cited"
    },
    {
      "a": "story:commit-and-revision-lineage",
      "b": "story:version-persisted-contracts",
      "path": "crates/ekr-store/src",
      "confidence": "cited"
    },
    {
      "a": "story:commit-and-revision-lineage",
      "b": "story:version-persisted-contracts",
      "path": "crates/ekr-store/tests",
      "confidence": "cited"
    },
    {
      "a": "story:commit-and-revision-lineage",
      "b": "story:version-persisted-contracts",
      "path": "crates/ekr/tests",
      "confidence": "cited"
    }
  ],
  "unassessed": []
}
```

### `aep plan artifact waves --kind story --status draft --format json`, reduced by `jq` to ids, scope counts, collisions and unassessed

```json
{
  "waves": [
    {
      "wave": 1,
      "ids": [
        {
          "id": "story:seed-and-explain",
          "scope_entries": 4,
          "inferred": false
        }
      ]
    },
    {
      "wave": 2,
      "ids": [
        {
          "id": "story:ekr-cli",
          "scope_entries": 10,
          "inferred": true
        }
      ]
    },
    {
      "wave": 3,
      "ids": [
        {
          "id": "story:ess-conformance-kernel",
          "scope_entries": 11,
          "inferred": true
        }
      ]
    }
  ],
  "collisions": [
    {
      "a": "story:ekr-cli",
      "b": "story:ess-conformance-kernel",
      "path": "Cargo.lock",
      "confidence": "cited"
    },
    {
      "a": "story:ekr-cli",
      "b": "story:ess-conformance-kernel",
      "path": "crates/ekr/Cargo.toml",
      "confidence": "cited"
    },
    {
      "a": "story:ekr-cli",
      "b": "story:ess-conformance-kernel",
      "path": "systems/ekr/domains/kernel.yaml",
      "confidence": "cited"
    },
    {
      "a": "story:ekr-cli",
      "b": "story:seed-and-explain",
      "path": "systems/ekr/domains/kernel.yaml",
      "confidence": "cited"
    },
    {
      "a": "story:ess-conformance-kernel",
      "b": "story:seed-and-explain",
      "path": "systems/ekr/domains/kernel.yaml",
      "confidence": "cited"
    }
  ],
  "unassessed": []
}
```
