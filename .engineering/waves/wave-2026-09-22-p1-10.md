# P1-10: source guards, synchronous runtime refusal and provider adoption

This closes prerequisites of the approved completion plan. It does not activate
new persisted formats or implement the durable writer. The source guard scope is
p1-guard-debt-scope.md; runtime scope/evidence is p1-runtime-context.md.
The coordinator completed the COORDINATOR checks and owns planning/publication.

## Source and recovery

- Guard unit: `codex/ekr-p1-10-guards-20260922`, source 07a299f, tree
  `<worktrees>/ekr-p1-10-guards-20260922`; build `<cache>/b10x-target/ekr-p1-10-guards-20260922`.
- Runtime unit: `codex/ekr-p1-runtime-context-20260922`, source 51c7511,
  tree `<worktrees>/ekr-p1-runtime-context-20260922`. Its idle build output was
  removed with Cargo after retaining evidence and integrating the source.
- Integration: `codex/ekr-completion-20260922`, tree and target recorded in
  COMPLETION-2026-09-22.md. Guard merges after the runtime source and exact
  published Eventlog pin 4ee3dc23f0d02a5726a0e41d097477791f09efe2.
- Scratch: `<cache>/ekr-completion-20260922/guard-debt`, `runtime-context`
  and `tmp/eventlog-adoption-*`. Unit cleanup awaits source publication.

## Review and correction

The implementation, first adversary review, correction and correction re-review
are retained as `.engineering/reviews/p1-10-guards-*`. The first review's normal
Cargo comment escape and brace-delimited macro escape were blockers; genuine
generic/array type use was a false-positive class. The shared bounded member
reader and token recognition corrections preserve every original adversary case.
Unsupported member syntax refuses explicitly. API coverage remains a lexical
guard; it does not claim full Rust name resolution.

The re-review reports 468 passed, zero failed and zero ignored, with formatting
and strict Clippy green. It added two independent controls and confirmed exact
preservation of the original six cases. The coordinator then bypassed comment
removal in the member reader: the new quoted-path/inventory case failed with one
executed failure, exit101. Exact SHA-256 restoration passed and that case passed
again. No mutation remains. The original runtime-checkout witness also retains
the same binary's false green before repair, distinguishing red after repair and
green after restoring the second checkout.

The separately reviewed runtime change rejects an entered Tokio context before
provider/authority/I/O work and safely drops an owned runtime there. Its review,
restored coordinator mutation and exact behavior are in p1-runtime-context.md.
The adopted Eventlog source supplies atomic blob/event publication and strict
read-only inspection. Consumer dependency checks and kernel/store tests passed;
EKR's new blob placement and format activation are still future work.

## Acceptance and state

Existing compile-fail cases survive unchanged; AGENTS.md now states the bounded
diagnostic refresh policy for incidental trait-help changes. The seed-boundary
residual task is closed against the published kernel seed cases. The complete
integration gate must run after combining these changes, before publication and
closure of the guard/runtime/provider-adoption tasks.

Owners: first guard review, three introduced implementor findings; correction
re-review, zero findings. Runtime review, zero implementation findings, with its
one read-only charter deviation disclosed in the runtime wave page. Coordinator
owns integration evidence, policy documentation and release decisions. This host
does not expose per-agent token/tool totals; none is estimated.

## Integration gate

The actual `task check` command exited 0
on the combined source. All six sequential task steps appear exactly once in
the complete retained log and completed successfully: format, strict Clippy,
workspace tests, rustdoc, ESS validation and AEP validation. Individual status
files are derived from that successful sequential execution, not a second run.

Runner totals: 479 passed, 0 failed, 0 ignored across 79 runner summaries.
The executable macro guard passed against the integration checkout. The combined
source includes both providers and the unchanged original-format vectors.
The retained-result compiler work, V2 activation and durable writer remain open.

## Publication and cleanup

PR #10 merged the exact candidate 9a15b96368161047c15783503f8e0ea0c70dd101.
Required correctness run 35691309048, job106628687643, completed every step,
including the actual repository gate; common Security/privacy and signed local
evidence also succeeded on that head. The bot fast-forwarded main only after
those checks, and the primary checkout is clean and synchronized.

The guard and runtime unit commits are retained by the published history.
Their leases were released, task-owned inactive build directories were cleaned
with Cargo, and each managed tree passed finish and exact-ID GC dry-run before
GC removed it. Both merged local unit branches were deleted after ancestry checks.
Private command logs, mutation evidence and review reports remain retained.
