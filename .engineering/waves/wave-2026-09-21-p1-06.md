# Wave 2026-09-21 p1-06 — the two invariants, repaired

Skill version 0.9.3. Coordinator: the session that ran p1-01 through p1-05. Operator approved the
plan *The next ten waves* (2026-09-21), which names this as wave 1 of ten. Interactive run,
pre-approved: the stage-1 stop is waived, the page is not.

**N = 1.** Both repairs touch `crates/ekr-store/tests/fixture/mod.rs`, so no second unit can run
beside this one.

## Why this wave exists, and it is not in any story

An independent review on Fable over `56abc2b..f293b4d` found that **two of the three exit criteria
for this phase are carried by nothing**, and the coordinator had reported both as met.

| invariant | what the review measured |
|---|---|
| **1** — only a `ValidatedTransaction` commits | a commit lands at revision 1 in a process that **cannot link `ekr-kernel`**, on three hand-written events through the public `RevisionLog::append`. `ValidatedTransaction` has zero consumers in any `src/`. The store's own suite pins the negation green |
| **2** — a `Canonical → Transient` reference is unrepresentable at the type level | the sealed machinery is sound and **no field of `Node`, `Edge`, `Assertion` or `CanonicalGraph` has that type**. A canonical edge into a transient root compiles, constructs, and is refused by the kernel — the word the sentence excludes |

Both are the coordinator's, and the second sentence of invariant 1 was unimplementable as written:
`ekr-store` sits **below** `ekr-kernel`, so its writer cannot take a `ValidatedTransaction`, and a
sealed trait in the store excludes the kernel along with everybody else.

`architecture-decision-record:0007-the-commit-path-is-the-kernels` and
`…:0008-canonical-state-references-are-typed` settle both, accepted before dispatch.

## The exit criteria are the reviewer's own cases, not a claim about them

Three of its nine red cases are copied into this tree and are the unit's to turn green:

| case | file | finding |
|---|---|---|
| `a_commit_lands_with_no_validated_transaction_anywhere_in_the_process` | `crates/ekr-store/tests/review_p1_invariant_one_at_the_store.rs` | A |
| `a_commit_validated_against_a_revision_that_is_no_longer_the_head_replays_as_valid` | same file | E |
| `the_sealed_reference_types_are_sound_and_canonical_state_holds_none_of_them` + its `trybuild` case, and `canonical_state_holds_an_edge_into_a_transient_root_and_no_type_refuses_it` | `crates/ekr-graph/tests/review_p1_membrane_is_by_id.rs`, `tests/review_p1_compile_fail/` | B |

Finding E rides with this wave rather than the debt wave because it is a three-line change in
`log.rs`, which this wave already rewrites, and it shares a file with A.

**Held for wave 2** — six more red cases in three files left in the review tree, because this wave
does not fix them and a red gate cannot merge: `review_p1_the_seed_is_validated_by_nobody.rs` (C),
`review_p1_validation_hash_domain.rs` (D), `review_p1_kernel_revision_projection.rs` (F).

## Commits this wave authorises

| # | commit | branch |
|---|---|---|
| 1 | opening store commit: this page, both ADRs, both tasks to `active`, the `AGENTS.md` correction, the three exit-criterion cases | `wave/p1-06` |
| 2 | one unit commit | `impl/invariant-repair` |
| 3 | closing store commit: gate evidence, both tasks `implemented`, scope confirmed | `wave/p1-06` |
| 4 | the merge of the pull request that brings `wave/p1-06` into `main` | `main` |

**This branch also carries `c4e436e`, which is not this wave's.** It is the planning commit made on
`main` before the wave opened; direct pushes to `main` are refused by repository rule, so it reaches
`main` through this wave's pull request. It is named here so the commit list stays honest.

## The `AGENTS.md` correction, and it is the coordinator's third wrong sentence in two days

`AGENTS.md:85-86` said unit artifacts are keyed by a hash including the manifest path, "so two
trees' `deps/` do not clobber each other". **Measured false.** The reviewer's probe: two
byte-identical crates of one name in two directories, one shared build directory — `deps/` holds
**one** binary for both, and the second directory's `cargo test` ran the first's in 0.01s and
reported the first's compile-time path.

So the clause is struck, the reuse it contradicted is stated with the clobber as its mechanism, and
`cargo clean -p` is recorded as a move rather than a fix. Counts corrected to **20 files, 39 uses**,
including `xtask/src/main.rs:32`, which is not a test.

## Unit

| | |
|---|---|
| artifacts | `task:only-a-validated-transaction-commits`, `task:canonical-state-references-are-typed` |
| branch | `impl/invariant-repair`, forked from `wave/p1-06` |
| worktree | managed id `ekr-impl-invariants`, `<worktrees>/ekr-impl-invariants` |
| build dir | `$HOME/.cache/b10x-target/ekr-p1-06-invariants` — its own |
| scratch | `$HOME/.cache/ekr-wave-1/unit-scratch` |
| stage | dispatched |
| head | — |
| subagent_type | `aep-drive:implementor`, then `aep-drive:adversary`, both on Opus |

Integration checkout: `wave/p1-06` from `main` at `c4e436e`; managed id `ekr-wave-p1-06`,
coordinator lease `ekr-coordinator-p1-06`.

## Pre-flight

| check | reading |
|---|---|
| primary on `main`, synced | `c4e436e`; **ahead of `origin/main` by one**, which this wave's pull request carries |
| working tree | clean at wave creation; the three paths that were dirty are in `c4e436e` |
| `git worktree list` | 2 entries — the primary and `ekr-review-p1`, which is the independent review's and **not a previous wave's**. It is finished and is removed at this wave's close |
| free disk | 44G (floor 10G) |
| build directories | 5, 10.6G total; one is another repository's and is not touched |
| compiler cache | `sccache` present at `/usr/bin/sccache`, `RUSTC_WRAPPER` unset — accepted at N = 1 |
| `aep` | protocol 0.55.0 |
| `AGENTS.md` | read, and corrected by this wave |

## Gate

`task check` on `wave/p1-06` after the unit merges, one exit status per step.

## Release

Not part of this wave.
