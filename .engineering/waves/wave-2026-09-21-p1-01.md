# Wave 2026-09-21 p1-01 — the P1 crate skeleton

Skill version 0.9.3. Coordinator: the Claude session that decomposed P1 on 2026-09-21. Operator
instruction, in session: "then select waves and use /aep-drive:wave to implement wave by wave,
stop at any" (2026-09-21). Interactive run: the operator is present, so the stage-1 stop is taken.

N = 1. `aep plan artifact waves` puts exactly one story in wave 1; every other P1 story
`depends_on` it, transitively.

## Commits this wave authorises

| # | commit | branch |
|---|---|---|
| 0 | `chore: bootstrap the repository (P0)` — the P0 tree (14 untracked paths) as the first commit; a wave branches from a commit and `main` has none | `main` |
| 1 | opening store commit: this page, the story moved to `active` with its `serves` edge | `wave/p1-01` |
| 2 | one unit commit | `impl/workspace-crate-skeleton` |
| 3 | `Merge impl/workspace-crate-skeleton: …` | `wave/p1-01` |
| 4 | closing store commit: gate evidence, story `implemented`, scope confirmed | `wave/p1-01` |
| 5 | `Merge wave/p1-01: …` | `main` |

Nothing else: no push, no tag, no release. Commit 0 is outside the skill's list and is named here
so that approving the wave visibly approves it.

## Selection

`aep plan artifact waves --kind story`, protocol 0.55.0, after the round-2 revision:

```text
wave 1
  story:workspace-crate-skeleton (inferred)
wave 2
  story:kernel-identity-and-hashing (inferred)
wave 3
  story:ontology-types-and-values (inferred)
wave 4
  story:graph-model-and-assertions (inferred)
wave 5
  story:eventlog-store (inferred)
  story:transaction-and-validators (inferred)
wave 6
  story:commit-and-revision-lineage (inferred)
wave 7
  story:seed-and-explain (inferred)
wave 8
  story:ekr-cli (inferred)
wave 9
  story:ess-conformance-kernel (inferred)
collision: story:ekr-cli story:workspace-crate-skeleton crates/ekr/src/main.rs (inferred)
collision: story:eventlog-store story:workspace-crate-skeleton crates/ekr-store/src/lib.rs (inferred)
collision: story:graph-model-and-assertions story:workspace-crate-skeleton crates/ekr-graph/src/lib.rs (inferred)
collision: story:kernel-identity-and-hashing story:workspace-crate-skeleton crates/ekr-kernel/src/lib.rs (inferred)
collision: story:ontology-types-and-values story:workspace-crate-skeleton crates/ekr-ontology/src/lib.rs (inferred)
9 wave(s), 5 collision(s), 0 unassessed
```

Selection path: the verb. Every collision is between the wave-1 story and a later wave, and the
`depends_on` edges already sequence them; none is live for this wave.

**Scope is `inferred` for every story, and scoped by the coordinator rather than a
`story-scoper`.** No crate exists yet; the surfaces are the files the stories will create, so
there is nothing in the tree a scoper could cite. Recorded as a deviation. Each implementor
confirms its scope in its report and the closing step writes the confirmation back.

Blast radius: the workspace manifest, the lockfile and six empty crates. One package each, no
logic.

## Critic panel

Two rounds, four critics each (`aep-plan:plan-critic-acceptance`, `-design`, `-scope`,
`-parallel-safety`), recorded as `review-result:p1-<lane>-round-<n>`. Round 1: 13 findings on 10
stories, 13 outcomes `fixed`. Round 2: acceptance 4 findings, parallel-safety 1, design 1, scope
`approve`; all 6 round-2 findings `fixed` in the second revision. Per the rubric there is no third
round; the round-2 fixes stand unreviewed by a critic and are named as such.

The design finding of round 2 changed the crate map: `ekr-kernel` (ids, hashes) and `ekr-graph`
(types the validators and commit read) would have been a two-way Cargo dependency. A bottom crate
`ekr-core` now holds identity, hashing and canonical encoding; `ekr-kernel` sits above `ekr-graph`
and `ekr-store`. `docs/roadmap.md` § 3 carries the acyclic order. The wave-1 story therefore
creates six crates, not five.

## Store edges added to leave draft

`serves vision:o2` on `story:workspace-crate-skeleton`; moved `draft → proposed → active` in the
opening store commit. `aep plan artifact validate` relayed there.

## Unit

| | |
|---|---|
| story | `story:workspace-crate-skeleton` |
| acceptance | `task check` exits zero with `ekr-core`, `ekr-kernel`, `ekr-ontology`, `ekr-graph`, `ekr-store` and `ekr` present as workspace members |
| branch | `impl/workspace-crate-skeleton`, forked from `wave/p1-01` |
| worktree | created with `worktree create`; path recorded here at creation |
| build dir | `$HOME/.cache/b10x-target/epistemic-knowledge-runtime` — shared per `AGENTS.md` § The gate (this repository's rule; the skill's per-tree default loses to it). N = 1, so no two trees build at once |
| scratch | `$HOME/.cache/ekr-wave-p1-01/unit-scratch` |
| stage | proposed |
| head | — |
| subagent_type | `aep-drive:implementor`, then `aep-drive:adversary` |

Integration checkout: `wave/p1-01`, forked from `main` at `0ed1108` (commit 0); managed id `ekr-wave-p1-01`, path `$HOME/.local/state/worktree/trees/b10x/epistemic-knowledge-runtime/ekr-wave-p1-01`, coordinator lease `ekr-coordinator-be67fda9`.

## Pre-flight

| check | reading |
|---|---|
| primary on `main`, clean | on `main`; **0 commits**; 14 porcelain lines, all untracked, all P0 output. Commit 0 above clears this; nothing is stashed or discarded |
| `git worktree list` | 1 entry, the primary |
| free disk | 38G on `/` (floor 10G). Fell from 42G to 38G during this session from processes outside this repository; this repository's build directory is 73M |
| one measured build | cold `cargo build --workspace` of the scaffold: 3.0s, 59M; warm `task check`: 0.34s, exit 0 |
| build cache | `sccache` at `/usr/bin/sccache`, `RUSTC_WRAPPER` unset — accepted at N = 1 |
| model budget | not stated by the operator; N = 1, so the default of 4 does not bind |
| `AGENTS.md` | read; its invariants go into the unit brief unchanged |

## Gate

`task check` (fmt, clippy `-D warnings`, test, doc, `ess specify validate`, `aep plan artifact validate`),
run once on `wave/p1-01` after the merge, one exit status per step.

## Release

Not part of this wave. 0.1.0 is P7's exit, through the bot route, after a person decides.
